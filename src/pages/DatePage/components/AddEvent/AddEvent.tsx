import React, { FC, useRef } from "react";
import { Box, Button, useMediaQuery } from "@mui/material";
import { styles } from "./AddEvent.styled";

export const AddEvent: FC = () => {
  return <Box sx={styles.calendarContainer}>add</Box>;
};
