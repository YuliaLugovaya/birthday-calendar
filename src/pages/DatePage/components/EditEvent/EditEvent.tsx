import React, { FC, useRef } from "react";
import { Box, Button, useMediaQuery } from "@mui/material";
import { styles } from "./EditEvent.styled";

export const EditEvent: FC = () => {
  return <Box sx={styles.calendarContainer}>edit</Box>;
};
